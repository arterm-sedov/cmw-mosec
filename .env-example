# =============================================================================
# CMW-Infinity Configuration
# =============================================================================
# This file contains server configuration for cmw-infinity.
# Model slugs are case-insensitive.
#
# Available models (run: cmw-infinity list):
#   Embeddings:
#     - ai-forever/FRIDA (4GB VRAM, port 7997)
#     - Qwen/Qwen3-Embedding-0.6B (2GB VRAM, port 7997)
#     - Qwen/Qwen3-Embedding-4B (12GB VRAM, port 7997)
#     - Qwen/Qwen3-Embedding-8B (22GB VRAM, port 7997)
#
#   Rerankers:
#     - DiTy/cross-encoder-russian-msmarco (2GB VRAM, port 7998)
#     - BAAI/bge-reranker-v2-m3 (2GB VRAM, port 7998)
#     - Qwen/Qwen3-Reranker-0.6B (2GB VRAM, port 7998)
#     - Qwen/Qwen3-Reranker-4B (12GB VRAM, port 7998)
#     - Qwen/Qwen3-Reranker-8B (22GB VRAM, port 7998)
#
# Usage:
#   cmw-infinity start "Qwen/Qwen3-Embedding-8B"
#   cmw-infinity start dity/cross-encoder-russian-msmarco  # Case insensitive
# =============================================================================

# =============================================================================
# Server Settings
# =============================================================================

# Default device: auto (GPU if available), cpu, or cuda:N
DEVICE=auto

# Default dtype: float16 (recommended), float32, int8
DTYPE=float16

# Default batch size (adjust based on VRAM)
BATCH_SIZE=32

# =============================================================================
# Logging
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# =============================================================================
# Runtime Configuration (optional overrides)
# =============================================================================

# Override default port for a specific model
# PORT=8080

# GPU memory limit (optional, for multi-GPU setups)
# MAX_MEMORY_ALLOCATED=20GB
